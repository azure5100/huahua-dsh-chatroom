# Node / package managers
node_modules/
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# patch-weave.ps1 runtime artifacts (auto-backup of pre-patch target file)
*.bak-portfix
*.bak

# Internal orchestration docs — kept out of the public release.
# ASSET-INVENTORY.md is the team's working source-of-truth and carries
# pre-sanitization references (host names, addresses, instance ids).
docs/ASSET-INVENTORY.md
# REVIEW.md is the reviewer's compliance working doc — also carries
# pre-sanitization token references; on-disk only, never published.
docs/REVIEW.md
# REVIEW-FINAL.md is the reviewer's final audit working doc — same rule.
docs/REVIEW-FINAL.md

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/
.vscode/
